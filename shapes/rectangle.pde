void setup () {
  size(1920, 1080);
  background(255);
}

void draw () {
  fill(255, 0, 255); //filling the shape with a RGB color
  stroke(0); //black stroke
  rect(250, 200, 30, 60); //rectangle function with (x, y, width, height) //(x,y) is the left top angle of the rectangle
}
